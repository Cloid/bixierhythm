using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ButtonControllerKeys3D : MonoBehaviour
{
    // Author: Herald Hamor
    // Public Variables
    public KeyCode keyToPress;
    // Private Variables

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // If key is pressed, then indicate the button has been pressed in some way
    }
}
